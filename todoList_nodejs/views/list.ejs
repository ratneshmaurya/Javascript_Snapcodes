<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/styles.css">
</head>
<body>
    <div class="box" id="heading">
            <h1><%= kindOfDay %></h1>
    </div>
    <!-- here kindOfDay is the ejs variable that taking value from app.js -->
    <!-- we access passing value by using <%= %> in ejs -->
    <!-- we have to use <% %> in ejs for any javascript line that contains any js words -->
    <div class="box" >
            <% Newitems.map((item)=>{  %>
                <!-- when we check the checkbox it submit the form and make post request to /submit page -->
                <form action="/submit" method="post">
                    <div class="item">
                        <input type="checkbox" name="checkbox" value="<%= item._id %>" onChange="this.form.submit()">
                        <p> <%= item.name %> </p>
                    </div>
                </form>  
            <% })%>

        <form class="item" action="/" method="post">
            <input type="text" name="newItem" autocomplete="off" placeholder="Add items"></input>
            <button type="submit">+</button>
        </form>
    </div>

</body>
</html>